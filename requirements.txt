# Project dependencies (Python 3.12), fully pinned for reproducibility.
# Create the environment with:
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# numpy is pinned to 2.1.2 for compatibility with numba (a shap dependency).

# Data pipeline and testing
numpy==2.1.2
pandas==3.0.3
pyarrow==24.0.0
lz4==4.4.5
PyYAML==6.0.3
pytest==9.1.1

# Execution environments and agents
gymnasium==1.2.1          # Gym interface for the execution environments
torch==2.12.1             # Stable-Baselines3 backend (CPU/MPS on macOS arm64)
stable-baselines3==2.9.0  # DQN and PPO; supports gymnasium 1.x and native n-step DQN

# Evaluation and analysis
scipy==1.18.0             # Wilcoxon signed-rank test and paired bootstrap
scikit-learn==1.9.0       # analysis utilities
matplotlib==3.11.0        # figures and training-curve plots
shap==0.52.0              # per-regime feature attribution
numba==0.65.1             # shap dependency; pinned for numpy 2.1.2 compatibility
